local options = {
  ensure_installed = { "lua", "vim", "python", "go", "javascript", "yaml" },

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
